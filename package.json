{
  "name": "live-trader",
  "version": "1.0.0",
  "description": "A system to algorithmically trade.",
  "main": "server/app.js",
  "scripts": {
    "install-client-and-build": "cd client && npm install && npm run build",
    "install-server": "cd server && npm install",
    "build": "npm install && run install-client-and-build && npm run install-server",
    "start": "cd server && tsc && node dist/app.js",
    "test": "jest --coverage"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/austin-starks/Live-Trader.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/austin-starks/Live-Trader/issues"
  },
  "homepage": "https://github.com/austin-starks/Live-Trader#readme",
  "dependencies": {
    "concurrently": "^6.3.0",
    "typescript": "^4.9.4"
  },
  "devDependencies": {
    "prettier-plugin-organize-imports": "^2.3.4"
  }
}
